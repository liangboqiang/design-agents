name: worker_agent
root_skill: general/root
description: Thin worker agent that shares the same runtime and governance stack.
toolboxes:
  - files
  - shell
  - textops
capabilities:
  - todo
  - task
  - compact
  - autonomy
llm:
  provider: mock
  model: mock
context_policy:
  max_prompt_chars: 16000

